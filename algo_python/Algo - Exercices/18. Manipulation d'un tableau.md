Deviner le résultat des instructions d'affichage de l'algorithme suivant :

```algo
VAR notes[4] : REEL = [14.5, 0, 18, 4.5]

AFFICHER(LONGUEUR(notes))         # Affiche : ?

SUPPRIMER(notes[2])

AFFICHER(LONGUEUR(notes))         # Affiche : ?

# Quel est la nouvelle apparence du tableau ? 
# [?]

SUPPRIMER(notes[0])
INSERER(10.5, notes)
SUPPRIMER(notes[1])

AFFICHER(LONGUEUR(notes))         # Affiche : ?

# Quel est la nouvelle apparence du tableau ? 
# [?]

notes[0] = 10
notes[1] = 18
AFFICHER(notes[LONGUEUR - 2])    # Affiche : ?

AFFICHER(notes[LONGUEUR(notes)]) # Affiche : ?
```