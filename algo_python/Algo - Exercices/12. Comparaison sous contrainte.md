Construire l'algorithme suivant :

```algo
# Saisir deux nombres entiers
# Afficher
    # Soit le plus grand des deux nombres
    # Soit un message indiquant que les deux nombres sont égaux
# Contrainte : Utiliser une variable booléenne 
# pour séparer la phase de traitement (comparaison des nombres) 
# de la phase d'affichage afin de respecter 
# la structure algorithmique suivante :
    # DÉCLARATION (déclaration et typage)
    # INITIALISATION (affectation des saisies et valeurs par défaut)
    # TRAITEMENT (opérations de comparaison, de calcul)
    # AFFICHAGE DU MESSAGE (affiche du message utilisateur final)
```